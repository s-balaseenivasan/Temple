# next-auth lists nodemailer ^7||^8 as an *optional* peer; the app uses
# nodemailer 10 directly (src/lib/email), not via next-auth, so the peer
# range does not apply. Needed for npm ci on Render.
legacy-peer-deps=true
